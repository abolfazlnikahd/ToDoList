[tool.poetry]
name = "todolist"
version = "0.1.0"
description = "ToDoList - SQLAlchemy + Postgres"
authors = ["Your Name <you@example.com>"]

[tool.poetry.dependencies]
python = "^3.11"
SQLAlchemy = "^2.0"
psycopg2-binary = "^2.9"
python-dotenv = "^1.0"
alembic = "^1.11"
schedule = "^1.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
